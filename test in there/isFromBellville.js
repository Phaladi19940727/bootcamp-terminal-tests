/// isFromBellville.js

// Function to check if a registration number is from Bellville (starts with 'CY')
function isFromBellville(registrationNumber) {
  return registrationNumber.startsWith('CY');
}

module.exports = isFromBellville;

